pipeline {
    agent any
    
    tools {
        jdk 'JAVA11'
        maven 'Maven392'
    }
    
    stages {
            stage('Which Java?') {
                steps {
                    sh 'java --version'
                }
            }
            stage ('Build') {
                steps {                    
                    sh "mvn clean verify"                    
                }
            }

    }
}